# scripts/jobs/ — TSUBAME 作业脚本（版本控制，SD-8）
#
# 结构约定（EF-1 未裁决前必须保持调度器无关）：
#   <name>.body.sh    纯 bash 业务逻辑，可直接本地执行
#   <name>.qsub       Grid Engine 薄壳（#$ 指令）
#   <name>.sbatch     Slurm 薄壳（#SBATCH 指令）
#
# 无论 EF-1 结论如何，.body.sh 零改动。
# 每个作业脚本必须实现 Sprint_Plan §8.2 Step 4：显式 cd 绝对路径、
# 覆盖 RAP_ROOT、dirty 检查拒绝运行、归档 job.script 与 hashes.json。
